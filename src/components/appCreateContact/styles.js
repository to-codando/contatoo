export default (root, props) =>   /*css*/`
    ${root} { display:inline }  

    ${root} .ctx-form {
        display:flex;
        justify-content:flex-end;
        align-items: flex-start;
        flex-wrap: wrap;
        width:100%;
        padding:2rem;
        max-width: 720px;
        margin:3rem auto 0 auto;
        border: 1px #C1CBD9 solid;
        border-radius:10px;
    }


`
